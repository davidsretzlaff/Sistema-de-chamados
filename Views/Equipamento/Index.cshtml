@model IEnumerable<ChamadosPro.Models.Equipamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdEquipamento)
        </th>

    </tr>

@foreach (var item in Model) {
    <tr>
        <th>
            @Html.DisplayFor(modelItem => item.IdEquipamento)
        </th>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEquipamento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEquipamento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEquipamento })
        </td>
    </tr>
}

</table>
